<h1 class="pageHeader">Hello $user.getChildName()!</h1>
<div class="search">
    <form action="/symbols/search_results/child_view?user_id=$user.getId()&timetable_id=$timetable.getId()" class="form-search-box" method="post">
        <input class="user-form input-search" type="text" name="search" id="input-search" placeholder="Find a symbol...">
        <button type="submit"><i class="fas fa-search"></i></button>
    </form>
</div>
<div class="most-used">
    <div class="frequent-item"><a href="/users/$user.getId()/view_symbol/child_view?symbol_id=$symbol1.getId()&timetable_id=$timetable.getId()"><img class="border-on-images" src="$symbol1.getImageUrl()"/></a></div>
    <div class="frequent-item"><a href="/users/$user.getId()/view_symbol/child_view?symbol_id=$symbol2.getId()&timetable_id=$timetable.getId()"><img class="border-on-images" src="$symbol2.getImageUrl()"/></a></div>
    <div class="frequent-item"><a href="/users/$user.getId()/view_symbol/child_view?symbol_id=$symbol3.getId()&timetable_id=$timetable.getId()"><img class="border-on-images" src="$symbol3.getImageUrl()"/></a></div>
</div>
<hr class="hr">
<div class="categories">

    <ul>
        #foreach($category in $categories)
        <li><a href="/symbols/search_results/child_view?searchQuery=$category.getDescriptor()&user_id=$user.getId()&timetable_id=$timetable.getId()"><span><i class="category-icon $category.getIcon()"></i></span>$category.getDescriptor()</a><hr class="hr"></li>
        #end
    </ul>
</div>
